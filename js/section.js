jQuery(function($){
	// Mask click handler
	// var PowerCheck = function() {
	// 	var n = parseInt(this.value, 10);
	// 	if (n & (n-1)) this.value = '';
	// };

	$('.MaskOption').duplicate({addButton: '.AddDescription'});
});